{
  "name": "kami",
  "private": true,
  "version": "0.1.0",
  "description": "Kami Community Edition — self-hosted AI GTM agency",
  "scripts": {
    "dev": "npm --prefix web run dev",
    "build": "npm --prefix web run build",
    "eval:sales": "npm --prefix web run eval:sales",
    "eval:e2e": "npm --prefix web run eval:e2e",
    "readiness": "node scripts/readiness.mjs",
    "sync:skills": "node scripts/sync-hermes-skills.mjs"
  }
}
